Inputs: in, sel;
Outputs: out1, out2;

Parts:
    not1 NOT,
    and1 AND,
    nand1 NAND;

Wires:
    in -> nand1.in1,
    sel -> nand1.in2,

    nand1.out -> not1.in,
    
    nand1.out -> and1.in1,
    in -> and1.in2,

    and1.out -> out1,
    not1.out -> out2;